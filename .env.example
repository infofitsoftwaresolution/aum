AWS_REGION=ap-south-1
AWS_SECRETS_NAME=aum-report-secrets
# AWS access keys are only needed for local development.
# In production on EC2, rely on the instance IAM role instead.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
OUTPUT_DIR=output
LOG_LEVEL=INFO

